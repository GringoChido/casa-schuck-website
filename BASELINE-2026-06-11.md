# Phase 0 Baseline — June 11, 2026

Recorded before any relaunch-prompt changes. Compare against this at final verification.

## Build
- `npm run build` passes. 41 static pages (13 routes × 2 locales + root redirect + sitemap + 12 blog paths).
- Routes: /, /[locale] (home, blog, blog/[slug], experiences, groups, packages, privacy, retreats, services, story, suites, terms, weddings), /sitemap.xml

## JS bundle
- `.next/static/chunks` total: **1.0 MB**
- Largest chunks: 224 KB, 119 KB, 117 KB, 113 KB, then long tail under 35 KB
- `'use client'` directives in src: **37** (target after Phase 3: < 15)

## Images
- `public/images` total: **183 MB**
- Files over 500 KB: **89**
- Worst offenders: areas/dining.jpg (~21 MB), areas/living-room.jpg (~19 MB), areas/rooftop.jpg (~12 MB)

## Impeccable detector (June 11 pre-run, 85/92 files readable)
1. [single-font] src/app/[locale]/layout.tsx — only Material Symbols Outlined detected as loaded font
2. [layout-transition] src/app/globals.css ~411 — `transition: height`
3. [side-tab] src/components/sections/RetreatHub.tsx ~107 — `border-l-4`

## Fresh detector run (Phase 0, full file set)
Confirmed exactly the 3 known findings — nothing new from the rehydrated homepage.

Fixed in Phase 0:
- [layout-transition] globals.css `.room-tab-border::before` now animates `transform: scaleY()` instead of `height` (same visual behavior)
- [side-tab] RetreatHub.tsx cards: `border-l-4 border-secondary` replaced with a dusty-rose hairline full border; `bg-white` → `bg-surface-bright` (Plaster Rule)

Remaining after Phase 0: **1 finding** — [single-font] clears when Material Symbols → Lucide migration lands (Phase 3, only 2 files use it: InRoomComforts.tsx, HeroSplit.tsx).

## Impeccable context
PRODUCT.md, DESIGN.md, and DESIGN.json written at repo root (register: brand, North Star: "The Golden-Hour Courtyard").

## Pre-existing observation (not changed)
`HotelJsonLd` / `LocalBusinessJsonLd` / `WebSiteJsonLd` already render in `[locale]/layout.tsx` — the June review's "no Hotel JSON-LD" finding was stale. Phase 2 validates/extends the shapes instead of creating them.
